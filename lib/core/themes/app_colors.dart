import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF1A1A2E);
  static const primaryLight = Color(0xFF16213E);
  static const accent = Color(0xFFE94560);

  // Background
  static const background = Color(0xFFF8F9FA);
  static const surface = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);
  static const textWhite = Color(0xFFFFFFFF);

  // Border
  static const border = Color(0xFFE5E7EB);
  static const borderFocus = Color(0xFF1A1A2E);

  // Status
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const offline = Color(0xFFEF4444);

  // Source badge
  static const sourceBadgeBackground = Color(0xFFEEF2FF);
  static const sourceBadgeText = Color(0xFF4F46E5);

  // Shimmer
  static const shimmerBase = Color(0xFFE5E7EB);
  static const shimmerHighlight = Color(0xFFF9FAFB);

  // Overlay
  static const imageOverlay = Color(0x80000000);
  static const cardShadow = Color(0x1A000000);
}